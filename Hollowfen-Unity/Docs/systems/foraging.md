# Foraging System
Walk-up mushroom interaction: HUD prompt → cinematic focus cam → Inspect journal screen (rotating 3D preview) → Forage/Leave, with per-species discovery gating, harvest cinematic, and persisted inventory.
Key scripts: `Assets/_Hollowfen/Scripts/Foraging/` — MushroomNode, PlayerInteractor, InspectScreen, InventoryScreen, MushroomPreviewer, InventoryRuntime, MushroomDiscovery, MushroomFocusCamera, IInteractable.
Data: `MushroomFieldGuideData` SOs at `Assets/_Hollowfen/Data/Mushrooms/` (20 species, ALL now tied to real fungi with real Latin + photos — educational-game rule). The T4 trio (18–20) are Sable's seedbook folk-names for real deadly/psychoactive species (batch-20): Moonring = Destroying Angel (Amanita virosa), Hollowheart = Death Cap (Amanita phalloides), Wendlight = Liberty Cap (Psilocybe semilanceata), reusing entries 02/06/05's photos. Moonring/Hollowheart entry-only; Wendlight forageable in the Old Wend (⚠️ its glowing world prefab still reads mystical — a Meshy/model follow-up to match Liberty Cap).
Entry points: `Player/Interact` (E / Triangle) opens Inspect; `Player/OpenInventory` (I / Square) opens Provisions.
Biggest gotchas: disable Wren's PlayerInput while screens are open (buffered-Jump bug); silhouette material for undiscovered species; Meshy prefab pivot/scale recipe.
Status: shipped + play-mode verified (Act I). Node respawn field exists but is disabled.

> Self-healing doc: if you change this system, update this doc (including the 7-line header) in the same batch, and note the change in the batch worksheet.

---

## Components

| Component | Path | Role |
|---|---|---|
| `IInteractable` | `_Hollowfen/Scripts/Foraging/IInteractable.cs` | Tiny contract — `PromptVerb`, `PromptTarget`, `CanInteract`, `Interact`. Future-proofs NPCs / chests. |
| `MushroomNode` | `_Hollowfen/Scripts/Foraging/MushroomNode.cs` | On the world prefab. References `MushroomFieldGuideData`. `Interact()` opens `InspectScreen`; `Harvest()` is called by the screen — logs, fires `OnAnyHarvested`, marks discovery, fires `ACH_FORAGE_FIRST` once, deactivates self. Optional `_respawnSeconds` field exists but disabled. |
| `PlayerInteractor` | `_Hollowfen/Scripts/Foraging/PlayerInteractor.cs` | On `PlayerArmature`. Each frame `Physics.OverlapSphereNonAlloc` on the Foraging layer from a chest-height offset, picks the closest `IInteractable` inside a forward cone (dot ≥ -0.2), raises `OnFocusChanged`. Subscribes to `Player/Interact` and calls `current.Interact(gameObject)`. Static `Suspended` flag — any screen that takes input ownership flips it true and search/interact go quiet (HUD fades out automatically). |
| `MushroomDiscovery` | `_Hollowfen/Scripts/Foraging/MushroomDiscovery.cs` | Static. PlayerPrefs-backed registry of discovered species IDs (`forage.discoveredIds` as `;`-joined list). Lazy-loaded on first access. Fires `OnDiscovered` event. |
| `MushroomPreviewer` | `_Hollowfen/Scripts/Foraging/MushroomPreviewer.cs` | Singleton scene component at `(0, -1000, 0)`. Builds its rig in `Awake()` — orthographic camera + key/fill directional lights + turntable mount. Renders to a 1024² ARGB32 RT (4× MSAA). RT background is warm `Parchment` cream so the model reads as a specimen on journal paper. `Show(data, silhouette)` instantiates the SO's `WorldPrefab` onto the mount, recursively re-layers it to `MushroomPreview`, and (when `silhouette=true`) replaces every renderer's `sharedMaterials` with a flat URP-Unlit dark walnut material — used for undiscovered mushrooms so first-encounter inspections show shape + scale only. `ApplyRotationDelta` (yaw + clamped pitch ±80°) and `ApplyZoomDelta` (clamped ortho size) drive manual control; user input disables `AutoRotate` until the next `Show()`. Zoom range `_minOrthoSize = 0.005 .. _maxOrthoSize = 0.50` gives ~26× zoom-in from default 0.13. `SetBackgroundColor(c)` re-applies the camera bg at runtime. |
| `InspectScreen` | `_Hollowfen/Scripts/Foraging/InspectScreen.cs` | Scene-local screen (Map-style, NOT a `UIScreen`). Singleton built programmatically from `UICanvasUtil` + `HollowfenPalette`. Parchment journal layout — 1500×940 panel with a `[SerializeField] Sprite _parchmentSprite` background (falls back to flat `Parchment` color), vertical-gradient vignette overlay, outer + inner gold-faint double-rule frame. Top: gold "FIELD JOURNAL · SPECIMEN" eyebrow. Left: 700×700 preview frame with thin gold inset, RawImage of the previewer RT, hint pill at the TOP of the frame. Right: edibility eyebrow, Georgia 68px serif title in `InkDeep`, gold underline rule, italic Latin in `Moss`, edibility chip, body description, three-column stat strip (HABITAT / SEASON / LOOK-ALIKES, height 170 to fit safety copy), italic gold Forager's note pull-quote. Bottom: solid gold Forage button + outlined parchment Leave button with auto-detected gamepad glyph pills. `RefreshButtonGlyphs()` runs each frame so swapping pads mid-session updates live. Pauses with `Time.timeScale = 0`, sets `PlayerInteractor.Suspended = true`, releases mouse cursor. `Update()` reads `Mouse.current` + `Gamepad.current` directly (right stick rotate, RT/LT zoom; mouse drag rotate inside preview rect, scroll-wheel zoom anywhere). |
| `InteractionPromptHUD` | `_Hollowfen/Scripts/UI/InteractionPromptHUD.cs` | Bottom-center pill under `_HUDCanvas`. Builds itself programmatically. CanvasGroup-driven 120ms fade on unscaled time. Subscribes to static `PlayerInteractor.OnFocusChanged`. Renders `[E / △]  {verb} {target}` — verb routed through `Localization.Get` (key on `MushroomNode.PromptVerb`), target read raw from `data.CommonName`. |
| `InventoryRuntime` | `_Hollowfen/Scripts/Foraging/InventoryRuntime.cs` | Static, in-memory dictionary of mushroom-id → count. `Add(data, n)` / `Remove(data, n)` mutate counts and fire `OnChanged(id, newCount)`. Lazy-hydrates from `SaveSlotMeta.Inventory` on first read (autosave slot 0). `Persist()` calls `SaveManager.AutoSaveInventory(snapshot)` after every change — every harvest = one tiny JSON write. SO refs cached in a parallel `_byId` map so UIs can render photos without re-resolving every frame. |
| `InventoryInputBridge` | `_Hollowfen/Scripts/Foraging/InventoryInputBridge.cs` | One-line bridge — subscribes to `Player/OpenInventory` and toggles the screen. Refuses to open if `InspectScreen.IsOpen` so we never stack two modal screens. |
| `InventoryScreen` | `_Hollowfen/Scripts/Foraging/InventoryScreen.cs` | Scene-local screen at `sortingOrder = 55` (below Inspect's 60). Same parchment journal panel as InspectScreen. Title "Provisions" with gold underline. Empty-state copy when `DistinctCount == 0`. Otherwise: 4-col `GridLayoutGroup` of mushroom cards (170×220) in a `RectMask2D` ScrollRect (752×660) on the left; selected card recolors its 4-edge border to gold; click / D-pad selection drives `MushroomPreviewer.Show(cell.Data, false)`. Right: 600×600 preview frame with hint pill at top, "MODEL COMING SOON" overlay when `data.WorldPrefab == null`, selected-mushroom title + Latin + count badge underneath. Bottom: single Close button. Subscribes to `InventoryRuntime.OnChanged` so harvests while open refresh live. **Inspect Mode toggle**: R1 / Tab flips between Browse (gamepad navigates cards, camera locked) and Inspect (camera unlocked: left-stick pans, right-stick rotates, LT/RT zooms; EventSystem selection cleared so left-stick is freed). Cancel routes through Inspect Mode first (B exits inspect; second B closes). |
| `MushroomFocusCamera` | `_Hollowfen/Scripts/Foraging/MushroomFocusCamera.cs` | Cinematic "discovery shot" via Cinemachine 3. Single `CinemachineCamera` + `CinemachineFollow` + `CinemachineRotationComposer` on `_MushroomFocusCamera`. Default priority 5 (below `PlayerFollowCamera`'s 10); when `PlayerInteractor.OnFocusChanged` fires with a non-null target, raises priority to 20 → CinemachineBrain blends to the framed shot at `_blendSeconds = 0.55`. Lens FOV `38°`, follow offset `(0.7, 0.7, -1.6)` for over-the-shoulder hero framing. Manual orbit: right-stick / RMB-drag yaw + clamped pitch (`-10°..60°`). UI/Cancel calls `PlayerInteractor.DismissCurrent()` to back out. Exposes `IsHarvestCinematicActive` so `MushroomNode.HarvestCinematic` can own FollowOffset during pickup. |

## Gotchas / conventions

- **Localization keys, not raw strings, on `IInteractable.PromptVerb`.** `MushroomNode.PromptVerb => "prompt.inspect.verb"`; the HUD calls `Localization.Get` on it. Keeps the HUD a single funnel for new verbs (NPC "Talk", chest "Open", etc.).
- **Two layers added**: `Foraging` (slot 8) hosts only the trigger SphereCollider on the prefab root — the inner `Mesh` child with the convex MeshCollider stays on `Default` so movement physics doesn't collide with the focus-search lane. `MushroomPreview` (slot 9) isolates the previewer's camera + spawned model from the main camera.
- **PlayerArmature was already on layer 8** from the StarterAssets template. OverlapSphere returns the player's own collider every frame but `GetComponentInParent<IInteractable>` filters it out — slightly wasteful, not a bug.
- **InspectScreen auto-creates an EventSystem** if none exists (so playing directly into `Scene_Hollowfen` works) and turns off `InputSystemUIInputModule.deselectOnBackgroundClick`. `Update()` re-applies the last-focused button if `currentSelectedGameObject` goes null.
- **Forage shortcut redundancy**: inside the inspect screen, `Player/Interact` (Triangle / E) is a Forage shortcut, `UI/Submit` activates the focused button, `UI/Cancel` leaves. Same Triangle button opens the screen and forages — feels continuous.
- **URP material recipe for Meshy mushrooms**: pack `metallic.rgb + (1 - roughness).a` into a `*_MaskMap.png` (sRGB off, `alphaSource = FromInput`), feed to URP Lit `_MetallicGlossMap` with `_SmoothnessTextureChannel = 0` and `_METALLICSPECGLOSSMAP` keyword.
- **Mushroom prefab pivot + scale**: Meshy FBX exports a ~1cm mesh inside a `localScale=100` transform. Wrap in a clean parent at root `localScale=0.08` → final ~16cm tall. Trigger `SphereCollider` on the parent (local radius 18 = 1.44m world at scale 0.08). `MeshCollider(convex)` on the inner Mesh child so it inherits the 100× transform.
- **`MushroomPreviewer.Update()` uses `Time.unscaledDeltaTime`** so auto-rotate keeps spinning while the inspect screen is paused.
- **Silhouette material is created lazily** on first `Show(data, silhouette=true)` — `Shader.Find("Universal Render Pipeline/Unlit")` with `_BaseColor` (+ URP-fallback `_Color`) near-black walnut. Reverting is automatic — the discovered path re-instantiates the prefab with original materials.
- **Inspect-screen layout dims**: panel 1500×940, preview 700×700 anchored `(56, 60)` panel-local (upper half — clears the centered button row at `(0, 36)` from panel bottom), text column starts at `868` from panel left. Stat strip is 170 tall because `Mushroom_07_FieldMushroom` LOOK-ALIKES runs ~7 lines. Hint pill at TOP of preview frame (bottom collided with buttons at high widths).
- **Gamepad glyph detection** uses `Gamepad.current.GetType().Name` + `pad.description.product.ToLowerInvariant()` matched against PS / Xbox / Switch / generic. Default fallback is PS-style (`△`/`○`). Glyphs refresh every frame.
- **Foraging gating**: every encounter routes through Inspect — no quick-pickup. Discovery state lives in `MushroomDiscovery`.
- **First-harvest achievement** (`ACH_FORAGE_FIRST`) is PlayerPrefs-gated by `forage.firstHarvestSeen`.
- **Vertex count is 414k** on the Field Mushroom Meshy export. Heavy; flagged for a decimation pass before EA (see TODOS.md).
- **Inventory persistence**: `MushroomNode.Harvest` → `InventoryRuntime.Add(_data, 1)` → snapshot written to `saves/slot0.json` immediately. On gameplay launch, `InventoryRuntime.EnsureHydrated` reads it back. Dev reset: delete `Application.persistentDataPath/saves/slot0.json` + `PlayerPrefs.DeleteAll()`.
- **Cinematic harvest sequence** (`MushroomNode.BeginHarvest`): Forage button triggers this instead of synchronous Harvest. Closes screen via `HideForCinematic` (deliberately doesn't restore time/Suspended/cursor — the coroutine owns them). Coroutine on `Time.unscaledDeltaTime`: phase 1 (0.30s) lerps focus-cam FollowOffset to 55% (dolly in) under `Time.timeScale = 0.5` slow-mo; phase 2 (0.80s) lifts 25cm + spins 360°/s + scales to 5%; commit (log/events/discovery/inventory/achievement) happens BEFORE `SetActive(false)` so coroutine cleanup runs on an active host.
- **`PlayerInteractor.SetPlayerInputEnabled(bool)`** toggles Wren's StarterAssets `PlayerInput`. Used by InspectScreen, InventoryScreen, and the harvest cinematic. Without it, Cross/Space confirms Forage AND queues a buffered `Player/Jump` that fires when timeScale resumes — Wren jumps after every harvest.
- **`PlayerInteractor.DismissCurrent()`** backs out of cinematic zoom (`MushroomFocusCamera.OnCancel`). Saves the target into `_suppressed`, clears focus; suppression clears once the target leaves OverlapSphere range.
- **Oyster placement**: mounted to trunks via `Quaternion.FromToRotation(Vector3.up, outwardNormal)` — Meshy's Oyster growth axis is local +Y. `Mushroom_17_Oyster` was authored from scratch (web import had 16 species) as the 17th database entry.
- **Preview centering**: Meshy exports pivot at mesh BASE. `MushroomPreviewer.Show` computes renderer bounds and shifts the child so `bounds.center == mount.position` — otherwise the camera frames the stem base.
- **Camera pan in previewer**: `ApplyPanDelta(Vector2)` shifts camera position AND look target by the same world-space vector along `cam.right/up` — true lateral pan, composes with rotate + zoom. `ResetView()` zeroes pan/yaw/pitch/ortho on every `Show()`.
