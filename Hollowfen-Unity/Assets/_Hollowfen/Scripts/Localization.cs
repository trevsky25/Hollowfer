namespace Hollowfen
{
    // Placeholder LUT — returns the string ID itself. Real translation happens
    // before EA launch. Target launch languages: English, Simplified Chinese.
    public static class Localization
    {
        public static string Get(string stringId) => stringId;
    }
}
